inherit:
  - models/standard.yaml
  - wandb.yaml

model:
  n_dims: 20
  n_positions: 20   # For 9 points: 2 * 4 + 1 = 9, need n_positions >= 18

training:
  task: group_mixture_linear
  data: group_mixture_linear
  task_kwargs:
    n_components: 1            # Just 1 component = pure linear regression (no clustering needed)
    contexts_per_component: 4  # 4 context points
    noise_std: 0.0
    scale: 0.5
  batch_size: 64
  learning_rate: 0.0001
  save_every_steps: 1000
  keep_every_steps: 100000
  train_steps: 5001
  curriculum:
    dims:
      start: 20
      end: 20
      inc: 1
      interval: 2000
    points:
      start: 5   # T = 1 * 4 + 1 = 5
      end: 5
      inc: 2
      interval: 2000

out_dir: ../models/group_mixture_linear_simple

wandb:
  name: "group_mixture_linear_1comp"

test_run: False
